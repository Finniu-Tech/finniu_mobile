import 'package:flutter/material.dart';

class InternetConnectionAlertWidget extends StatelessWidget {
  final Widget child;

  const InternetConnectionAlertWidget({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
