import 'package:flutter/material.dart';

class SliderDraft extends StatelessWidget {
  const SliderDraft({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width,
      height: 90,
      child: const Row(),
    );
  }
}
